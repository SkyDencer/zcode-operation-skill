# Problems — Open Issues & Resolved Issues

## Open Issues

IDs were renumbered to the zero-padded `P6-H-0NN` scheme on 2026-09-25 during
the Sub-Phase 6.4 report rewrite: old `P6-H1`–`P6-H13` are now `P6-H-001`–
`P6-H-013` in the same order, `P6-H-014`–`P6-H-019` are new High findings from
the 6.4 re-audit, `P6-H-020`–`P6-H-025` came out of the 6.5 documentation audit,
`P6-H-026` from the 6.9 reranker review, and `P6-H-027` from the 6.12 final
report. Open items as of 2026-09-26: **27 (21 High, 6 Medium)**.

| # | Date       | Problem                                                                        | Priority | Assigned To | Status   |
|---|------------|--------------------------------------------------------------------------------|----------|-------------|----------|
| P6-H-001 | 2026-09-24 | Route hook injects the whole 54-skill corpus into every prompt — `hooks/route.mjs:153` applies no `topK` and no score floor, so `rankSkills` returns all 54 entries (including zero-score ones) and all are written to `additionalContext`. Violates `docs/ai-context.md:502,521`. Not fixed in 6.4 because `tests/e2e/hook-process.mjs:122-123,157-158` asserts non-empty context for prompts that score 0.000. | High | routing owner | Open |
| P6-H-002 | 2026-09-24 | Context budget can be exceeded by design — `src/core/budget/manager.mjs:56-57` takes `max(quota, minPerSkill)`, so `minPerSkill × count` may exceed `maxChars`. Hook log: `injectedChars: 24972` vs `budgetMaxChars: 24000`. `tests/budget/manager.test.mjs:117-122` asserts the overflow. | High | routing owner | Open |
| P6-H-003 | 2026-09-24 | Raw prompts are persisted, contradicting the privacy invariant — `hooks/route.mjs:192,202,214,271` log `query: <raw prompt>` and `src/telemetry/session-tracker.mjs:118` persists `prompt.slice(0,60)`. `docs/ai-context.md:423,618` says raw prompts are never stored. | High | telemetry owner | Open |
| P6-H-004 | 2026-09-24 | Default benchmark mode contradicts the frozen baseline — `tests/run-benchmark.mjs:38` defaults to `hybrid` (Top-1 0.4385) while `--mode bm25` gives 0.9231, matching `data/baseline.json`; `package.json:14` runs it with no mode. | High | tooling owner | Open |
| P6-H-005 | 2026-09-24 | Deploy snapshot/rollback breaks on Windows separators — `src/deploy/writer.mjs:128` stores `join(dir,'SKILL.md')` (backslashes) but `:155` matches with `+ '/'` and `:347` splits on `'/'`; rollback then deletes newly created router dirs and `--restore` cannot rebuild them. Static evidence only (no deploy was executed in 6.4). | High | deploy owner | Open |
| P6-H-006 | 2026-09-25 | `feedback --outcomes` attribution is structurally dead — `src/cli/feedback.mjs:110-112` needs `decision.prompt`, but decision logs only persist `promptHash` (`src/telemetry/feedback.mjs:61`), so `--json` yields no `attributions` key. The separate `logDir` defect is fixed (Sub-Phase 6.1); the live CLI now reports 0/0/300 because all 608 signal rows are older than the 10-minute stale threshold (see resolved row 8). | High | feedback owner | Open |
| P6-H-007 | 2026-09-24 | Documented `SKILL_ROUTER_BM25_*` overrides never reach the scorer — `src/core/retriever/bm25.mjs:14,64`, `src/core/routing/detector.mjs:21,47`, `src/core/retriever/attribution.mjs:12`, `src/core/retriever/hybrid.mjs:16` read `getDefaults()` not `getConfig()`; `src/core/routing/hierarchical.mjs:19` destructures the weights and never uses them. | High | config owner | Open |
| P6-H-008 | 2026-09-24 | cwd-relative data paths, inconsistent with the Phase 3.5 fix — `src/core/telemetry/logger.mjs:9`, `src/telemetry/{feedback,signals,session-tracker}.mjs`, `src/analytics/{reader,analyzer}.mjs`, `src/config/defaults.mjs:25,46`, `src/core/routing/domain-registry.mjs:14`, `hooks/build-index.mjs:27-28`, `src/cli/reindex.mjs:15-16` use `resolve('logs'\|'data/…')`; running the hook from a foreign cwd wrote `logs/` into that cwd and hid `data/weights.json`. | High | infra owner | Open |
| P6-H-009 | 2026-09-24 | Generated index stores absolute personal paths — `data/skill-index.json` entries carry absolute user-local paths to `SKILL.md` files (git-ignored via `.gitignore:10`, but non-portable; `src/core/retriever/bm25.mjs:112-114` silently skips unresolvable paths). | High | index owner | Open |
| P6-H-010 | 2026-09-24 | Symlink-containment checks are dead code — `src/sync/planner.mjs:81-100`, `src/import/scanner.mjs:105-108`, `src/cli/import.mjs:33-38` use `stat()` (which follows links) then test `isSymbolicLink()` (always false). The documented guarantee (`docs/ai-context.md:330-331`) is not enforced. Runtime repro not possible on this host (`symlinkSync` → EPERM). | High | security review | Open |
| P6-H-011 | 2026-09-24 | `mergeWithEnv` can turn scalar config into arrays — `src/config/defaults.mjs:141-152` assigns the parsed array when the env value contains a comma (`SKILL_ROUTER_BM25_K1=1,2` → `cfg.bm25.k1 = [1,2]`), repeated in `src/config/env.mjs:66`. | High | config owner | Open |
| P6-H-012 | 2026-09-24 | `RoutePlan.mode` violates its contract — `hooks/route.mjs:221,287` emit `bm25`/`none`/`explicit`; `docs/ai-context.md:542` declares `"flat" \| "hierarchical" \| "single" \| "multi" \| "fallback"`. | High | hook owner | Open |
| P6-H-013 | 2026-09-24 | Query cache can never hit in production — `hooks/route.mjs:142` builds a fresh `QueryCache` per process invocation, so the `cache` telemetry event always reports 0 hits. | High | telemetry owner | Open |
| P6-H-014 | 2026-09-25 | Silent destructive failure in the deploy snapshot — `src/deploy/writer.mjs:110-143` wraps the whole mirror scan in `catch {}` (`:139`) and writes the snapshot unconditionally; `rollbackMirror()` (`:146-176`) then deletes every mirror directory not present in that (possibly empty) `files` array. A single `readdirSync` failure turns a failed deploy into a mass deletion that still reports "Rollback successful". | High | deploy owner | Open |
| P6-H-015 | 2026-09-25 | Silent destructive failure in the sync planner — `src/sync/planner.mjs:66-71` swallows `readdir` errors and returns an empty project index; `planSync()` then classifies every mirror skill as `remove` and `src/sync/writer.mjs:257` `rmSync`-es each one. A permission error on one project directory is indistinguishable from "the project has no skills". | High | sync owner | Open |
| P6-H-016 | 2026-09-25 | Silent misclassification of the whole outcome corpus — `src/telemetry/outcomes.mjs:132-155` wraps readdir plus the read loop in one `catch { // Directory doesn't exist }`; any other error (permissions, renamed logs dir) yields zero signals, and `correlate()` (`:87-90`) then classifies every decision `positive`. This is the failure mode Sub-Phase 6.1 fixed for the `logDir` argument, still reachable through any other error. | High | telemetry owner | Open |
| P6-H-017 | 2026-09-25 | `SKILL_ROUTER_SLM_ENABLED` never reaches the hybrid router — `src/core/routing/hybrid.mjs:29` reads `getDefaults()` instead of `getConfig()`, so the flag documented in `docs/ai-context.md:248` and in `docs/ai-context.md:597` is inert on the SLM path (the sibling `hierarchical.mjs:19` and `planner.mjs:16` already use `getConfig()`). | High | config owner | Open |
| P6-H-018 | 2026-09-25 | Legacy duplicate retriever kept alive only by the analytics CLI — `src/retriever.mjs:14,59` is a byte-level copy of `src/core/retriever/bm25.mjs:62,110` with hardcoded 3/2/1 field weights; `src/analytics/analyzer.mjs:18` imports it, so the `top10Skills` metric is computed with a different scorer than the live hook (which honours `data/weights.json`). | High | retrieval owner | Open |
| P6-H-019 | 2026-09-25 | Fully orphaned legacy logger — `src/logger.mjs:1-25` has zero importers (the only other reference is the string `'src/logger.mjs'` in the file-existence list at `src/cli/health.mjs:114`), writes an obsolete `logs/routing.jsonl` schema, and is the only module in `src/` with no JSDoc file header. | High | telemetry owner | Open |
| P6-H-020 | 2026-09-25 | No subcommand implements `--help`, and `deploy --help` is destructive — no `src/cli/*.mjs` inspects `--help`, so unrecognised flags fall through and the real command runs. `src/cli/deploy.mjs:27` defaults `withHook = true`, so `deploy --help` copies routers into the ZCode mirror and rewrites `~/.zcode/cli/config.json`. Reproduced in this session: `list --help` printed the 54-row table, `verify --help` ran the 5 checks and exited 1, `health --help` ran 8 checks and exited 2, `benchmark --help` ran the full 130-prompt benchmark and wrote `logs/benchmark-2026-09-25.json`; `deploy --help` was probed safely as `deploy --help --dry-run --zcode-dir <temp>` and printed a deploy plan, not help. | High | CLI owner | Open |
| P6-H-021 | 2026-09-25 | `--zcode-dir` and `--skills-dir` do not expand `~` — `src/cli/deploy.mjs:49` and `src/cli/verify.mjs:439-441` wrap the raw argv value in `resolve()`, and Node's `path.resolve` performs no tilde expansion on any platform. Verified: `resolve('~/.zcode/skills')` -> `<project>\~\.zcode\skills`. A documented example that passed `~` pointed every check at a directory named `~` inside the repo. The `~` examples have been removed from `docs/cli-reference.md` and `README.md` in Sub-Phase 6.5, but the flags still accept a silently wrong value. | Medium | CLI owner | Open |
| P6-H-022 | 2026-09-25 | `tune --json` is accepted and silently ignored — `src/cli/tune.mjs:123` sets `opts.json = true`, but no code path in `tune.mjs` or `tune-core.mjs` reads it, so `tune --json` prints the same human-readable text as `tune`. Documented as a real option until Sub-Phase 6.5 corrected the docs. | Medium | CLI owner | Open |
| P6-H-023 | 2026-09-25 | `SKILL_ROUTER_SOURCES` is documented as a general override but is read only by `hooks/build-index.mjs:39` — `src/cli/reindex.mjs:82-88` parses `--sources` but never the env var, `src/config/env.mjs` has no `SKILL_ROUTER_SOURCES` key, and `src/cli/sources.mjs:42` only echoes it. Verified: with the variable set, `sources` printed the value but the source table and counts were unchanged. `README.md` was corrected in Sub-Phase 6.5; the documentation/implementation gap itself remains. | Medium | config owner | Open |
| P6-H-024 | 2026-09-25 | `docs/ai-context.md` documented a fictional inverted-index schema (`format: "tedgram-skill-index-v1"`, `stats`, `index` with `postings`, `docs` with `manifestPath`) — the real `data/skill-index.json` is a flat array of skill objects with `name`/`description`/`keywords`/`domains`/`path`/`version`/`source` (verified by reading the file: `Array.isArray` true, 60 entries). `docs/cli-reference.md` and `docs/reports/phase-1.5-human-test-checklist.md:200` repeated the "BM25 inverted index" claim. Corrected in `docs/ai-context.md` and `docs/cli-reference.md` in Sub-Phase 6.5. | Medium | docs owner | Open |
| P6-H-025 | 2026-09-25 | The `feedback --outcomes` reading is not reproducible from the CLI — it is a function of wall-clock time relative to the on-disk signal timestamps, not a stable number. Measured four times within 40 seconds in Sub-Phase 6.5: 64/228/8, then 215/77/8, then 292/0/8 three times in a row, because `src/telemetry/outcomes.mjs:87-99` classifies a decision `positive` when no signal is within 10 minutes, `negative` when a retry/override/rephrase is within 5/2/5 minutes, and `unknown` only when signals exist but have all aged past 10 minutes. Every documented reading (0/0/300, 293/0/7, 196/99/5) therefore describes one instant. The `logDir` defect itself is fixed (P6-H-006); this is a property of the design. | Medium | feedback owner | Open |
| P6-H-026 | 2026-09-26 | The reranker feature weights do not generalise. `src/scripts/train-reranker-weights.mjs` fitted on the same `X, y` it scored, so the R-squared it reported (0.105, matching the log) was an in-sample fit statistic with no held-out estimate anywhere. Adding a 20%-by-prompt holdout in this sub-phase measures **held-out R² = -4.58** against an in-sample 0.079 (600 samples, 57 positives, 9.5%): the fit is worse than predicting the mean on unseen prompts. Corroborated end to end — `node tests/run-benchmark.mjs --mode hybrid --rerank on` scores 102/130 against 120/130 with reranking off. Other unmodelled limits: labels merge `expected` and `acceptable`; candidates are the hybrid top-20, so the reranker cannot rescue a skill ranked below 20; the weights are fitted against FNV-1a cosines but `src/core/reranker/engine.mjs` applies them regardless of the active provider. Disclosure only: the script now labels the in-sample figure and prints the held-out one, and `data/reranker-weights.json` was deliberately NOT regenerated (retraining is Sub-Phase 6.11's job, and shipping a newly fitted model whose generalisation is negative would be a regression). | High | retrieval owner | Open |
| P6-H-027 | 2026-09-26 | A tracked report reintroduces a personal path after the Sub-Phase 6.3 redaction. `docs/reports/phase-3-two-mode-benchmark.md:87-88` ends with two absolute paths under `C:\Users\PC-1\Desktop\projects\...`, and `git grep -F "PC-1" HEAD` still finds them. The file is rewritten by `tests/two-mode-benchmark/runner.mjs` on every run, so the redaction in `84317a6` did not survive the next benchmark run and re-applying it by hand would be undone the next time anyone runs the two-mode benchmark. Found while writing the Phase 6 final report; not fixed in that pass, which left the file byte-identical to HEAD. | Medium | docs owner | Open |

## Resolved Issues

| # | Date       | Problem                                                                        | Resolution                                      | Closed On  |
|---|------------|--------------------------------------------------------------------------------|--------------------------------------------------|------------|
| 1 | 2026-09-24 | routing.test.mjs: overall hit rate ≥ 50% (53/130)                              | Fixture drift — router skills pollute implicit routing. Test now uses leaf-only index matching hooks/route.mjs:136. Hit rate: 77/130 (59.2%). | 2026-09-24 |
| 2 | 2026-09-24 | hybrid.test.mjs: Recall@3 ≥ 95% on original prompts (18/20)                    | Fixture drift — router skills dilute embedding similarity. Test now uses leaf-only index. Recall@3: 19/20 (95%). | 2026-09-24 |
| 3 | 2026-09-24 | hybrid.test.mjs: hybrid Top-1 ≥ 90% on first 20 (7/20)                         | Stale assertion — FNV-1a n-gram embeddings do not improve over BM25 on this corpus. Threshold lowered to 55% (72/130) with WHY comment. | 2026-09-24 |
| 4 | 2026-09-24 | reranker.test.mjs: hybrid without rerank maintains baseline accuracy (7/20)    | Fixture drift — same router pollution issue. Test now uses leaf-only index. Hybrid gets 12/20 (60%). | 2026-09-24 |
| 5 | 2026-09-24 | P6-C1 (Sub-Phase 6.4): fractional BM25 field weights crashed retrieval — `src/core/retriever/bm25.mjs` and `src/core/routing/detector.mjs` passed the configured weight into `Array(n * w)` / `String.repeat(w)`, so `data/weights.json = {"name":2.71,"description":2.71,"keywords":0.57}` raised `RangeError: Invalid array length`. | Added `resolveFieldWeight()` (`src/scorer.mjs:31-45`) and shared `buildWeightedDocTokens()` (`src/core/retriever/bm25.mjs:20-49`); regression test `tests/retriever/field-weight-safety.test.mjs` (19/19). | 2026-09-24 |
| 6 | 2026-09-24 | P6-C2 (Sub-Phase 6.4): `require()` inside ESM modules broke two CLI commands — `src/cli/add.mjs:96` always returned an empty domain registry (every valid skill rejected, exit 1) and `src/cli/doctor.mjs:61-62` always reported the ZCode mirror as non-writable; the same pattern broke the synthetic-corpus path in `tests/run-benchmark.mjs` and `tests/integration/phase-2.mjs`. | Replaced with static `node:fs` / `node:child_process` imports; regression test `tests/cli/esm-require.test.mjs` (5/5) enforces "no `require(` in any `.mjs`". | 2026-09-24 |
| 7 | 2026-09-25 | P6-6.1: `tests/tuning/optimizer.test.mjs` asserted `top1 0.8615 >= 0.89` — corpus drift. The suite tuned the full 60-entry index (54 leaf + 6 `router-*`) while the hook's implicit BM25 path ranks a 54-leaf index (`hooks/route.mjs:139`); the 6 router dispatchers compete for prompts whose labels are leaf skills. | Leaf-only is canonical for implicit-routing confidence thresholds. Test passes `leafIndex` with an explanatory comment and keeps `fullIndex` to assert the measured router-pollution gap (leaf 0.9077 > full 0.8615). The standalone optimizer CLI (`src/tuning/optimizer.mjs:211-213`) and tuning-report CLI (`src/tuning/report.mjs:112-114`) now filter to the same leaf-only corpus so every entry point agrees with the runtime. `node tests/tuning/optimizer.test.mjs` → 44/44, `top1 0.9077`. | 2026-09-25 |
| 8 | 2026-09-25 | P6-6.1: the mission's `feedback --outcomes` criterion "non-zero negative count" is not reproducible from the current time-relative logs. On 2026-09-25 every row in `logs/signals-20260924.jsonl` predates the 10-minute stale threshold, so the live CLI correctly reports 0/0/300. The historical 196/99/5 came from 201 test-generated signals (Phase 5 report) and the file has since grown to 608 rows; replay counts are not expected to match. | Replay-time semantics left unchanged (no stale threshold was widened to manufacture a count). Proof of the `logDir` fix moved to a fixed-clock replay: `tests/telemetry/outcomes.test.mjs` section 8 replays each on-disk decision with `opts.now` pinned to its own corrective-signal timestamp and asserts a non-zero negative count (300/300 in this session). Discrepancy recorded in P6-H-006 above. | 2026-09-25 |
| 9 | 2026-09-25 | P6-C3 (Sub-Phase 6.4 security remediation): arbitrary file write via path traversal in the import/add path. `src/import/importer.mjs`, `src/cli/import.mjs` and `src/cli/add.mjs` built the destination directory from the untrusted SKILL.md frontmatter `name` (`name.split('-')` + `path.join`), so a skill named `backend-../../../../pwned` — which `validateSkill()` accepts because `backend` is a registered domain and the name starts with `backend-` — wrote `SKILL.md` outside `data/skills` (reproduced: `status: "imported"`, `score: 100`, file created outside the skills dir). The existing `hasTraversal()` guard could not fire: it was called on an already-`resolve()`d absolute skills directory (always false) and on `candidate.sourcePath`, never on the name. | Added `isSafeName()` / `isWithinRoot()` to `src/utils/fs.mjs`; both `resolveTargetDir()` implementations now return `null` for an unsafe name or an out-of-root target and the candidate is rejected (`field: "path"`); `src/cli/add.mjs` rejects the name before validation and re-checks containment; regression test `tests/security/path-traversal.test.mjs` (20/20) and `tests/security/cli-path-traversal.test.mjs` (10/10), which reproduced 19 failing assertions before the fix. The incorrect "guards are effective" claim in `docs/reports/phase-6-static-audit.md:372` was corrected. | 2026-09-25 |
| 10 | 2026-09-25 | P6-C4 (Sub-Phase 6.4 security remediation): second, independent instance of the same class — `disableSkill()` / `enableSkill()` joined the unvalidated `entry.path` onto the mirror root, reachable through unsanitised `--disable` names written into `.skill-router-disabled.json` (`src/cli/sync.mjs:40`). Shadow mode wrote `SKILL.md` to a sibling of the mirror root and mirror mode `rmSync`-ed a directory outside it (reproduced: `success: true`, files written/deleted outside the root). `src/sync/writer.mjs` had the same pattern for add/update/remove, with containment checked only on the add *source* and using string-prefix comparison. | `disableSkill()` and `enableSkill()` now verify the derived paths are inside the mirror (and project) root and fail closed; `writer.mjs` uses the shared `isWithinRoot()` and checks both source and mirror targets for add/update/remove, before any filesystem probe. Regression coverage: `tests/security/path-traversal.test.mjs` sections 4-6. | 2026-09-25 |
| 11 | 2026-09-25 | P6-6.4-repair-1: the two index builders disagreed on the default corpus, so `data/skill-index.json` depended on which one ran last. `src/cli/reindex.mjs:15` defaulted to `[{ data/skills }]` while `hooks/build-index.mjs:40-44` defaulted to `[data/skills, router-skills]`. Reproduced: `node bin/skill-router.mjs reindex` → "Indexed 54 skills", `routers= 0`; the next `tests/tuning/optimizer.test.mjs` then failed 3 assertions ("full index has 60 entries ... got 54", "full index has 6 router-* entries", "leaf-only top1 0.9077 > full-index top1 0.9077") because the leaf and "full" corpora were the same array. | Both builders now resolve the default project corpus through one shared `projectSources()` in `src/index/sources.mjs` (data/skills + router-skills, both `source: "project"`); `--skills-dir` remains the single-directory escape hatch and `SKILL_ROUTER_SOURCES` the explicit env override. Regression test `tests/cli/reindex.test.mjs` (12 assertions, 7 of which fail without the fix) is registered in both `package.json` chains; decision recorded as D26. | 2026-09-25 |
| 12 | 2026-09-25 | P6-6.5-doc-audit (first pass): 8 documentation consistency issues — broken links in docs/architecture.md:355 and docs/getting-started.md:308-314 (double-nested `./docs/` prefix), stale `npm run` examples in docs/skill-authoring.md:202,249,250 and docs/manager-playbook.md:24,40,41, missing README references to sync.md/troubleshooting.md/zcode-skill-visibility.md, missing `health` in src/cli/help.mjs, undocumented `SKILL_ROUTER_HIERARCHICAL_CONFIDENCE_THRESHOLD`, hardcoded personal path in docs/reports/phase-0.5b-deployment-20260921.md:3. | All fixed. The first pass's report at docs/reports/phase-6-doc-audit.md contained three claims that this pass found to be wrong and corrected in row 13 (the subcommand count of "20", the claim that `experimental` is not a real option, and the `--outcomes` reading of 0/0/300 stated as stable). | 2026-09-25 |
| 13 | 2026-09-25 | P6-6.5-doc-audit (second pass, this session): 20 code-to-doc mismatches (§4 of the report) and 10 stale examples (§5) remained after row 12, several of them high severity. (1) `docs/architecture.md:222` pointed at `src/core/routing/selector.mjs`, which does not exist. (2) The subcommand count was stated as 20 in four places; the real count is 18 (`node bin/skill-router.mjs help` lists 18; `src/cli/` holds 20 modules of which `tune-core.mjs` and `tune-guard.mjs` are helpers), and `docs/ai-context.md:447` said "18 total" while listing only 17 names — `deploy` was missing. (3) `bin/skill-router.mjs:8-22` listed 13 subcommands, omitted analytics/sources/feedback/health/tune, and called `import` a placeholder. (4) `docs/cli-reference.md:420` called `health` an "alias for `verify`" — it is a distinct command with 8 non-overlapping checks and its own 0/1/2 exit scheme. (5) `docs/cli-reference.md:265` said `--rollback <file>` "restores the mirror"; `src/cli/deploy.mjs:98-110` only parses the file and prints metadata, then returns. (6) `deploy --with-hook` was documented as the opt-in, but `src/cli/deploy.mjs:27` defaults `withHook = true` and registers on every non-dry-run deploy; the real opt-out `--no-hook` was undocumented. (7) `--mode` was documented as defaulting to `bm25`; `tests/run-benchmark.mjs:39` defaults to `hybrid`, confirmed by running `benchmark --help` (banner: `mode: hybrid`). (8) The benchmark example comment said "Real corpus (54 skills)" but the run reports `Index size: 60`. (9) `add` and `remove` were documented as running `reindex`; neither imports it, and both only print a tip. (10) `tune --json` was documented as an option; it is parsed and never read. (11) The Exit Codes table omitted 2, which `health` returns. (12) `feedback --outcomes` was undocumented. (13) `--zcode-dir ~/.zcode/skills` examples do not work — `path.resolve` does not expand `~`. (14) `deploy --list-snapshots` was documented with fabricated columns and a sample row; the real header is `Timestamp \| Mirror Root \| Ops` and a fresh checkout has no snapshots at all. (15) `deploy --restore` was documented as taking a file; it is a timestamp-prefix matcher. (16) The Global Options table was missing 10 real flags. (17) `docs/troubleshooting.md:85-88` used `JSON.parse` on an ES module, which throws. (18) `README.md` told users to `npm install` in a zero-dependency project and had a `git clone the repo` placeholder inside a ```bash fence. (19) `README.md` and `docs/getting-started.md` described install steps including a `deploy` that `scripts/install.mjs` never runs. (20) The `docs/getting-started.md` "Add a New Skill" walkthrough used an unregistered `mydomain` and a 35-token body, so the documented `add` command fails validation. (21) `docs/reports/phase-0.5b-deployment-20260921.md` was a 22-line stub with a self-referential first line and an unresolved TODO. (22) The `docs/ai-context.md` index schema described an inverted index that does not exist. | All 22 corrected in the documentation in this session. Each is listed with its evidence and file:line in docs/reports/phase-6-doc-audit.md. Six underlying code defects that the wrong docs were hiding were recorded as new open items P6-H-020 through P6-H-025. Module coverage re-verified: all 93 `.mjs` modules under src/, hooks/, bin/ and scripts/ are mentioned in at least one hand-written doc, so there were no coverage gaps to flag for mission item 6.5.6. | 2026-09-25 |
